#ifndef RENDERER_H
#define RENDERER_H

#include <SDL2/SDL.h>
#include <memory>
/*
namespace AcsGameEngine {

    class GameWindow;

    class Renderer {
    public:
        Renderer(std::unique_ptr<GameWindow> window);
        Renderer(const Renderer& orig);
        virtual ~Renderer();
    private:
        SDL_Renderer *m_renderer = nullptr;        
        std::unique_ptr<GameWindow> m_window;
    };
}
*/
#endif /* RENDERER_H */

